import React from 'react';
//import Item from './Item';

const List = (props) => {
    return (
        <h1>holi</h1>
        // <ul>
        //     {props.items.map((item, index) => {
        //         return <li key={item.id}>
        //             <Item item={item} />
        //         </li>
        //     })}

        // </ul>
    )
};

export default List;